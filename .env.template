# Copy to .env for local development
FLASK_SECRET_KEY=change-to-something-random
DEV_NETID=jcalford

# Seed external users on first startup (only created if username doesn't exist yet)
# Format: username:password:roles;username2:password2:roles2
# Roles are comma-separated: viewer,moderator
SEED_USERS=guckele:vtcancer:viewer
